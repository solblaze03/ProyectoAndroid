package com.example.practicarlogin.fuentes

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.practicarlogin.R

class Fuentes{
    companion object{
        val mulishBold = FontFamily(Font(R.font.mbold))
        val mulishLight = FontFamily(Font(R.font.mlight))
        val mulishMedium = FontFamily(Font(R.font.mmedium))
        val mulishRegular = FontFamily(Font(R.font.mregular))
        val mulishSemiBold = FontFamily(Font(R.font.msemi))
        val mulishExtraBold = FontFamily(Font(R.font.mulishbold))
    }
}